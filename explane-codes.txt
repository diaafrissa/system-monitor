code monitor-web.py
import psutil
    1• يقوم هذا السطر باستيراد مكتبة psutil، وهي مكتبة في بايثون تُستخدم للحصول على معلومات عن استخدام النظام، مثل استخدام المعالج، والذاكرة، والمساحة التخزينية
    . 
import time
    2• هنا، يتم استيراد مكتبة time، التي تقدم وظائف للتعامل مع الوقت، مثل الحصول على الوقت الحالي أو الانتظار لفترة معينة. 
    
from flask import Flask, jsonify, render_template
    3• يقوم هذا السطر باستيراد الكائنات المطلوبة من مكتبة Flask، وهي إطار عمل لتطوير تطبيقات الويب. يتم استيراد:
    
        ◦ Flask: لبدء تطبيق ويب. 
        ◦ jsonify: لتسهيل تحويل البيانات إلى تنسيق JSON. 
        ◦ render_template: لعرض ملفات القوالب (HTML). 
app = Flask(name)
    5• هنا، يتم إنشاء كائن من تطبيق Flask. يتم تمرير name كوسيلة لتحديد موقع التطبيق، مما يساعد Flask في العثور على ملفات القوالب والموارد الأخرى.
    
def get_system_metrics():
    6• هذا هو تعريف دالة جديدة تُسمى get_system_metrics. ستحصل هذه الدالة على معلومات النظام.
    
    
 return {
    7• يبدأ هنا بناء قاموس (dictionary) لإرجاع معلومات النظام. 
    
    
  "cpu_usage_percent": psutil.cpu_percent(interval=0.5),
    8• يقوم هذا السطر بجمع نسبة استخدام وحدة المعالجة المركزية (CPU) باستخدام الدالة cpu_percent من مكتبة psutil. تُستخدم interval=0.5 لتحديد فترة الانتظار لقياس الاستخدام على مدى نصف ثانية. 
    
  "memory_usage": psutil.virtual_memory()._asdict(),
    9• هنا، يتم جمع معلومات حول استخدام الذاكرة بنفس الطريقة. psutil.virtual_memory() تُرجع كائنًا يحتوي على معلومات الذاكرة مثل الذاكرة الكلية والفيديو المستخدم، و_asdict() تحول هذا الكائن إلى قاموس. 
    
  "disk_usage": psutil.disk_usage('/')._asdict(),
    10• يقوم هذا السطر بجمع معلومات استخدام القرص الصلب. يتم استخدام psutil.disk_usage('/') للحصول على معلومات حول القسم الجذري (/)، وتحويله إلى قاموس. 
  "timestamp": time.time()
    11• هنا، يتم إضافة الطابع الزمني الحالي (timestamp) باستخدام time.time()، والذي يمثل الوقت الحالي بالثواني منذ بداية الزمن (1 يناير 1970). 
 
    12• نهاية القاموس الذي يحتوي على بيانات النظام.    {
    
@app.route('/')
    13• هنا يتم استخدام الديكوريتور (decorator) @app.route لتحديد مسار (route) للتطبيق. المسار '/' يعني الصفحة الرئيسية. 
    
def index():
    14• هذا هو تعريف دالة index، والتي ستتعامل مع الطلبات المرسلة إلى المسار الرئيسي ('/'). 
    
 return render_template('index.html')
   15 • تقوم هذه الدالة بإرجاع ملف القالب index.html، مما يعني أن الصفحة الرئيسية للتطبيق ستعرض محتويات هذا الملف. 
   
@app.route('/api/metrics')
   16 • يعين مسارًا جديدًا ('/api/metrics') لإرجاع البيانات الإحصائية للنظام.
   
def api_metrics():
   17 • تعريف دالة جديدة تُسمى api_metrics، والتي ستتعامل مع الطلبات المرسلة إلى 'api/metrics'.
   
 return jsonify(get_system_metrics())
    18• هذه الدالة تعيد نتائج دالة get_system_metrics() على شكل JSON باستخدام jsonify. هذا يتيح للعملاء الوصول إلى معلومات النظام بطريقة منظمة وسهلة الاستخدام. 
    
if name == "main":
    19• يتحقق هذا السطر مما إذا كان البرنامج يتم تشغيله مباشرة (وليس مستوردًا كجزء من مكتبة أخرى).
   // hint : __name__: هو متغير مدمج في بايثون. قيمته تعتمد على كيفية استخدام الملف:

إذا تم تشغيل الملف مباشرة (مثلاً: python monitor_web.py)، فإن قيمة __name__ تُضبط إلى "__main__".

إذا تم استيراد الملف في ملف آخر (مثل import monitor_web)، فإن __name__ تُضبط إلى اسم الملف نفسه (بدون امتداد .py)، أي "monitor_web".

الهدف من الشرط: يتيح لك كتابة كود يمكن استيراده واستخدام دواله في ملفات أخرى دون أن يتم تنفيذ أجزاء معينة منه (مثل تشغيل الخادم أو تشغيل الاختبارات) عند الاستيراد. هذا مفيد جداً لتنظيم الكود وإعادة استخدامه.
خلاصة:
هذا السطر يسمح لك بجعل الملف:

قابلاً للتنفيذ عند تشغيله مباشرة.

قابلاً للاستيراد كوحدة دون تشغيل أي كود جانبي (side effects) لا ترغب به.




    
 app.run(host='0.0.0.0', port=5000, debug=False)
    20• يقوم بتشغيل التطبيق. يتم تعيين host على '0.0.0.0' مما يعني أن التطبيق سيكون متاحًا على جميع واجهات الشبكة، ويكون على المنفذ 5000. debug=False تشير إلى أن التطبيق لن يُشغّل في وضع التصحيح، مما يعني أنه لن يتم إعادة تشغيله تلقائيًا عند تغيير الشيفرة. 
